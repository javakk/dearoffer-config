server:
  port: 9001

spring:
  application:
    name: dearoffer-auth
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.230.128:3306/offer?characterEncoding=utf-8
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  redis:
    host: 192.168.230.128

  rabbitmq:
    host: 192.168.230.128

redis:
  keys:
    register_code_fix: 'register:code:phone_'

message:
  register:
    effectiveTime: 2

eureka:
  client:
    service‐url:
      defaultZone: http://localhost:8686/eureka
  instance:
    prefer‐ip‐address: true